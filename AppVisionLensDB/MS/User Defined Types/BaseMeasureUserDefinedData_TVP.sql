CREATE TYPE [MS].[BaseMeasureUserDefinedData_TVP] AS TABLE (
    [ServiceID]        INT           NOT NULL,
    [BaseMeasureID]    INT           NOT NULL,
    [BaseMeasureValue] VARCHAR (150) NOT NULL);

