CREATE TABLE [AVL].[ServiceAutoClassification_TicketUpload] (
    [ID]        INT           IDENTITY (1, 1) NOT NULL,
    [Ticket ID] NVARCHAR (50) NOT NULL,
    [ProjectID] BIGINT        NOT NULL
);

